$schema: 'https://moonrepo.dev/schemas/v2/tasks.json'

inheritedBy:
  tags: ['python']

fileGroups:
  sources:
    - 'src/**/*'
  configs:
    - 'pyproject.toml'
    - 'ruff.toml'
    - 'ty.toml'
    - '.python-version'
  tests:
    - 'tests/**/*'

tasks:
  install:
    command: 'uv sync'
    toolchains: 'system'
    inputs:
      - 'pyproject.toml'
      - 'uv.lock'
      - '.python-version'
    outputs:
      - '.venv'

  format:
    script: 'uv run ruff check --fix-only . && uv run ruff format .'
    toolchains: 'system'
    deps:
      - 'install'
    inputs:
      - '@globs(sources)'
      - '@globs(tests)'
      - '@files(configs)'

  lint:
    script: 'uv run ruff check . && uv run ty check'
    toolchains: 'system'
    deps:
      - 'install'
    inputs:
      - '@globs(sources)'
      - '@globs(tests)'
      - '@files(configs)'

  test:
    command: 'uv run pytest'
    toolchains: 'system'
    deps:
      - 'install'
    inputs:
      - '@globs(sources)'
      - '@globs(tests)'
      - '@files(configs)'

  build:
    command: 'uv build'
    toolchains: 'system'
    deps:
      - 'install'
    inputs:
      - '@globs(sources)'
      - '@files(configs)'
    outputs:
      - 'dist'

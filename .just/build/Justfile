set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Build every unit
all:
    moon run :build

# Build one unit
unit UNIT:
    moon run {{UNIT}}:build

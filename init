#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

project_name="$(basename "$repo_root")"

find . -name 'README.md' -not -path './.git/*' -delete

: > AGENTS.md

cat >README.md <<EOF
# ${project_name}
EOF

rm -rf .git
git init

rm -- "$0"

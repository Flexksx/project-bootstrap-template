set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# --- Add one recipe per unit, plus `all` once there's more than one, e.g.: ---
# cli:
#     go test ./apps/ponte/...
# webapp:
#     pnpm --dir apps/webapp-spa test
# all: cli webapp

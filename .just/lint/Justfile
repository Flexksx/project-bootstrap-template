set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Lint Markdown files
markdown:
    rumdl check .

# Run every linter
all: markdown

# --- Add per-stack recipes here as units land, e.g.: ---
# go:
#     golangci-lint run ./apps/backend-service/...
# ts:
#     pnpm --dir apps/webapp-spa exec biome lint .

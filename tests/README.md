# tests/

End-to-end / blackbox tests that span more than one unit in `apps/`.

Unit and integration tests for a single app live inside that app's own directory —
only cross-unit test suites belong here, e.g. `tests/backend-blackbox-test/`.

Delete this file once the first suite lands.

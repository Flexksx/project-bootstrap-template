# infra/

Infrastructure as code.

- `infra/projects/` — a project groups the deployables that ship together (e.g. one cloud project per environment).
- `infra/shared/` — infra shared across projects (networking, IAM, shared buckets).

Delete this file (and the two subdirectories, if unused) once real infra lands.

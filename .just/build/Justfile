set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Build everything
all *FLAGS:
    moon run :build {{FLAGS}}

# Build one unit
unit UNIT *FLAGS:
    moon run {{UNIT}}:build {{FLAGS}}
